[project]
name = "id2223-fine-tuning-llm"
version = "0.1.0"
description = "Fine tuning an LLM"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=7.1.0",
    "numpy>=2.3.5",
]
[tool.uv]
required-environments = [
    "sys_platform == 'darwin' and platform_machine == 'arm64'",
]
